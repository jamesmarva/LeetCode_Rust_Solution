fn main() {

}
use std::collections::HashMap;
pub fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> {
    let mut rst = Vec::with_capacity(2);
    let mut map: HashMap<u32, i32> = HashMap::new();
    for i in 0..nums.len() {
        if map.contains_key()
    }

}