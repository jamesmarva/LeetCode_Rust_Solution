fn main() {
    println!("Hello, world!");
}
use std::collections::HashMap;
pub fn group_anagrams(strs: Vec<String>) -> Vec<Vec<String>> {

    let rst: Vec<Vec<String>> = Vec::new();
    let map: HashMap<String, Vec<String>> = HashMap::new();
    
    for s in strs {

    }
    rst
}